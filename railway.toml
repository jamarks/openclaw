[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/usr/local/bin/docker-entrypoint.sh node openclaw.mjs gateway --allow-unconfigured --bind lan --port 18789 --config /tmp/.openclaw/openclaw.json"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
